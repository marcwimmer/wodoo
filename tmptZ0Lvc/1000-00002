networks:
  default:
    external:
      name: home_marc_odoo_default
services:
  browser:
    build: $ODOO_HOME/machines/browser
    cap_add:
    - SYS_ADMIN
    container_name: caetec11_browser
    env_file:
    - $ODOO_HOME/run/settings
    environment:
    - PASSWORD=odoo3000
    - GEOMETRY=1900x1200
    - DEPTH=24
    ports:
    - 5900:5901
    shm_size: 4gb
version: '3.5'

