networks:
  default:
    external:
      name: home_marc_odoo_default
services:
  cups:
    build: $ODOO_HOME/machines/cups
    container_name: caetec11_cups
    env_file:
    - $ODOO_HOME/run/settings
    environment:
      CONF_ROOT: /opt/printer_setup
      PRINTED_PATH: /opt/printed
      WATCHPATH: /opt/toprint
    ports:
    - 6631:631
    volumes:
    - cupsdatatoprint:/opt/toprint
    - $ODOO_HOME/data/cups/printed:/opt/printed
    - $ODOO_HOME/data/src/customs/caetec11/printer_setup:/opt/printer_setup
  odoo_base:
    env_file:
    - $ODOO_HOME/run/settings
    environment:
      CUPS_TOPRINT: /opt/toprint
    volumes:
    - cupsdatatoprint:/opt/toprint
version: '3.5'
volumes:
  cupsdatatoprint: null

