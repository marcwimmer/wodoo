#manage-order: 5000
version: '3.3'
volumes:
  ${DCPREFIX}_libreoffice_exchange:

services:
  libreoffice:
    container_name: ${DCPREFIX}_libreoffice
    build: $ODOO_HOME/machines/libreoffice
    environment:
      INPUT: /tmp/lo/1
      OUTPUT: /tmp/lo/2
    volumes:
      - ${DCPREFIX}_libreoffice_exchange:/tmp/lo
  odoo_base:
    volumes:
      - ${DCPREFIX}_libreoffice_exchange:/opt/libreoffice
    environment:
      LIBREOFFICE_SEND: /opt/libreoffice/1
      LIBREOFFICE_RECEIVE: /opt/libreoffice/2
    depends_on:
        - libreoffice
