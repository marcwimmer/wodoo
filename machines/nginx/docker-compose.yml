#manage-order: 5000
version: '3.3'
services:
  nginx:
    container_name: ${DCPREFIX}_nginx
    image: nginx:stable
    ports:
      - "${ODOO_PORT}:80"
    env_file:
        - $ODOO_HOME/settings
    volumes:
      - $ODOO_HOME/machines/nginx/nginx_${ODOO_VERSION}.conf:/mysite.template
      - $ODOO_HOME/run/nginx_paths:/etc/nginx/paths
      - $ODOO_HOME/run/nginx_confs:/etc/nginx/conf.d
      - $ODOO_HOME/machines/nginx/bin:/opt/bin
    command: /bin/bash /opt/bin/run.sh
