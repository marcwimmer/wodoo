<VirtualHost __DOMAIN__:80>
	ProxyPreserveHost On

	# order is important: so that /calendars is resolved before the all
	# catching odoo with /, it has to be in first place
	Include /etc/proxy/default/*.location
	Include /etc/proxy/odoo/__CONTAINERNAME__.location
</VirtualHost>
