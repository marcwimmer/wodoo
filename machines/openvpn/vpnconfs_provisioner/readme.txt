Purpose:

When setting up openvpn in the SNOM-Phones, a url is required where the 
phone fetches the vpn configuration tar archive.
This small webservice delivers the vpn config files independent from running
odoo directlry from client_out.
