settings {
	nodaemon = true,
	insist = true,
	maxDelays = 1,
} 
sync {default.rsyncssh,
	source = ".",
	host = "odoodev",
	targetdir = "/opt/odoo",
	exclude = { "dumps", ".git", "settings" },
	rsync = {
		compress = true,
	},
}
