settings {
	nodaemon = true,
	insist = true,
	maxDelays = 1,
} 
sync {default.rsyncssh,
	source = "/home/marc/odoo",
	host = "wc",
	targetdir = "/opt/odoo",
	exclude = { "dumps", ".git", "settings" },
	rsync = {
		compress = true,
	},
}
