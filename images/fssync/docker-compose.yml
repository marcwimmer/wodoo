# manage-order 8000
version: '3.3'
services:
  fssync:
    container_name: ${PROJECT_NAME}_${CUSTOMS}_fssync
    build: $ODOO_HOME/images/fssync
    restart: unless-stopped
    ports:
      - ${FSSYNC_LISTEN_IP}:14122:14122
    command: /run.sh
