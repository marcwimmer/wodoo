networks:
  default:
    external:
      name: home_marc_odoo_default
services:
  redis:
    container_name: caetec11_redis
    env_file:
    - $ODOO_HOME/run/settings
    image: redis
    volumes:
    - redis_persistent_data:/data
version: '3.5'
volumes:
  redis_persistent_data: null

